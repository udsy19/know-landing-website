# Convex
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Vly Email API (for OTP authentication)
VLY_EMAIL_API_KEY=your_vly_api_key_here

# Vercel Postgres (automatically set when you link a Postgres database in Vercel)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Notion API
# 1. Go to https://www.notion.so/my-integrations
# 2. Create new integration, copy the "Internal Integration Token"
NOTION_API_KEY=secret_xxxxxxxxxxxxx

# 3. Create a Notion database with these columns:
#    - Name (Title)
#    - Email (Email)
#    - Company (Text)
#    - LinkedIn (URL)
#    - Reason (Text)
#    - Signed Up (Date)
#    - Status (Select: New, Contacted, Converted)
# 4. Share the database with your integration (click Share → Invite)
# 5. Copy the database ID from the URL: notion.so/your-workspace/DATABASE_ID?v=...
NOTION_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
